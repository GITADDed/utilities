import line_equation

def import_commands(path):
    with open(path) as commands_file:
        commands = commands_file.read().split('\n')
    return commands

# input straights like [[point1x, point1y], [point2x, point2y]]
# return intersection_points like [[intersection_point1x, intersection_point1y], [intersection_point2x, intersection_point2y]]
def find_intersection_two_straight_with_straight(straight1, straight2, straight3):

    return intersection_points

def parse_command_to_point(command):

    return point
